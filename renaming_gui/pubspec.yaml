name: renaming_gui
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  file_picker: ^8.0.0+1
  path: ^1.9.0
  renaming_share:
    path: ../renaming_share
  desktop_drop: ^0.4.4
  logger: ^2.0.2+1  # 添加日志库

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
